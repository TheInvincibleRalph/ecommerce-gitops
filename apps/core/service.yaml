apiVersion: v1
kind: Service
metadata:
  name: core-monolith-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: core-monolith
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80